name: Update KernelSU-Next Submodule Pointer

on:
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: false

      - name: Set Git identity
        run: |
          git config user.name "Devandroid-bit"
          git config user.email "samirmami533@gmail.com"

      - name: Sync submodule
        run: git submodule sync --recursive

      - name: Initialize submodule
        # تم تغيير الاسم هنا ليطابق المسار الفعلي drivers/kernelsu
        run: git submodule update --init --force --recursive drivers/kernelsu

      - name: Checkout SuSFS Legacy commit
        run: |
          cd drivers/kernelsu
          git fetch origin
          # استخدام كود التخفي الصحيح لفرع legacy_susfs
          git checkout f448093b05ecbf9da4980391459afd0482540a0e
          cd ../..

      - name: Commit updated pointer
        run: |
          git add drivers/kernelsu
          git commit -m "Update KernelSU-Next to f448093 (legacy_susfs)" || echo "No changes to commit"
          git push origin main
